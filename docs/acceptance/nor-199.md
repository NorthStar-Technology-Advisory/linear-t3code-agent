# NOR-199 setup validation

Validated on 2026-09-16. The agreed scope is [first-time setup](../specs/first-time-setup.md).

## Fresh source walkthrough and real service checks

Copied the tracked source plus NOR-199 changes to a new temporary directory, with no node_modules or build output. Ran `npm ci` and `npm run build` successfully. Used a private 0600 environment file with the existing application settings and installation token store; no replacement app was created. npm reported three existing moderate dependency advisories. Setup does not run npm audit repair.

The installed T3Code CLI's `t3 auth session issue --help` exposes `--token-only`, `--ttl` and `--base-dir`. Issued a five-minute credential labelled for this read-only walkthrough, captured directly into the private environment file without logging it, and successfully read the live local T3Code project snapshot through doctor. Installed source maps and the current [official auth command source](https://github.com/pingdotgg/t3code/blob/main/apps/server/src/cli/auth.ts) confirm CLI-issued administrative bearer sessions. The documented onboarding command uses an explicit 30-day TTL; renew manually and preserve bridge state. No automatic bearer-refresh contract was assumed.

Opened the generated URL in the real signed-in Linear **Create new application** form. Confirmed the populated T3Code Agent name, description, repository website, callback URL and webhook URL; Webhooks, Issues and Agent session events were selected. Public distribution and client-credentials grant were not selected. Developer name remained empty, as intended. Did not submit the form or create another application. The form agrees with [Linear's supported manifest fields](https://linear.app/developers/oauth-app-manifests).

Doctor passed runtime configuration, Node/npm/git, GitHub CLI authentication, T3Code reachability/authentication/snapshot compatibility, actual installed Linear app identity, existing context-parent access and local bridge health.

The walkthrough **did not reach setup completion** against this deployment. Two existing prerequisites failed: NOR-199's Linear project did not have a valid selected T3Code project-label association, and the saved public HTTPS endpoint did not return bridge health. Doctor continued unrelated checks, reported the repair steps and exited 1. Effective selected-project settings/repository checks remained unverified as a consequence of the missing association. No labels, tunnel/proxy settings, deployment services or real application settings were changed to force a passing result. Complete the project-label association and repair the public HTTPS endpoint, then rerun setup/doctor to finish live acceptance. App creation and a new OAuth install were not repeated because the existing installation was reused.

This is evidence of the supported credential procedure, pre-filled form and live diagnostics, not a claim of completed fresh-install acceptance. No coding task, branch, worktree, push, PR, Linear message or delegation was started by the walkthrough. Real Linear webhook receipt and end-to-end execution remain unverified, as specified.

## Automated verification

Command subprocess tests use temporary private configuration, real temporary Git repositories, a fake Linear HTTP service and a fake T3Code HTTP/WebSocket service. They cover interrupted setup/rerun, completed reruns without another application link, secret generation/preservation/redaction, supported manifest parameters, URL validation, unrelated and multiline dotenv values, missing tools/configuration, auth and contract rejection, ambiguous/unknown titles, inaccessible repositories, unavailable effective settings, token scopes/expiry, explicit credential correction, connectivity timeout guidance, unchanged session/installation state, and honest completion wording. Requests are inspected for absence of mutations/coding work.

The required standards/spec reviews found and prompted fixes for multiline dotenv preservation, invalid-argument terminal cleanup and misclassified Linear timeouts. Those regressions are addressed before final verification. Broader delivery/restart/cancellation acceptance remains in [NOR-173](nor-173.md), [NOR-197](nor-197.md) and [NOR-198](nor-198.md).

Final verification: `npm run typecheck`, all **110 tests**, `npm run build` and `git diff --check` passed. An interactive-terminal check confirmed invalid `--replace` exits immediately with status 1. Final Standards review has no actionable findings; final Spec review has no remaining code defects and retains the live-completion limitation above.
