# NOR-173 acceptance record

## Automated and contract verification

Implementation exercises the signed webhook boundary against controlled Linear HTTP and T3Code HTTP/WebSocket services, SQLite recovery, temporary git repositories and an external PR-host boundary. Coverage includes routing, isolated sessions, duplicate delivery, ordered follow-ups, failure pause/resume, lost acknowledgements, event catch-up, correlated questions/approvals, stop signals, delayed provider stop, context refresh/pagination, explicit missing prerequisites, draft/incomplete reporting, concurrency and conservative cleanup.

Final verification on Node.js 24.18.0:

- `npm run typecheck` — passed for production code and tests.
- `npm test` — 32 passed, 0 failed, 0 skipped (including the live-discovered nullable-guidance regression).
- `npm run build` — passed.
- `git diff --check` — passed.

Focused tests were run through red/green implementation cycles. The full suite was run once after the review fixes.

Contract sources inspected on 2026-09-14:

- T3Code observed source commit `01e05c15268dedb76da95f442fbf5201cd8e7a44`; `packages/contracts/src/orchestration.ts`, `environmentHttp.ts`, provider ingestion, command receipts, snapshot projection and RPC handlers.
- T3Code's Effect RPC JSON wire protocol (`effect` 4.0.0-rc.112 as observed upstream), used only by the adapter's scoped event catch-up operation; this is not a T3Code compatibility pin.
- Linear's published GraphQL SDL, agent interaction and signal documentation.

## Standards

The independent standards review found no documented-standard violations. Its operational finding (unbounded replay-ticket retention) was fixed. Cancellation and thread-identity checks were consolidated, and definitive versus retryable integration failures now have explicit recovery behavior. The focused re-review confirmed all four findings resolved.

## Spec

The independent spec review found large-gap replay recovery and uncorrectable rejected answers. Both were fixed and covered at the signed-webhook boundary. Re-review identified multi-select answer support; request-specific validation now preserves valid arrays across restart, with a regression test. The final focused re-review found no outstanding code blocker.

Review totals: Standards 0 open findings; Spec 0 open code findings. The required core live exercise is now complete; supplementary live coverage limits are recorded below.

## Live acceptance — core exercise passed

Live setup on 2026-09-14:

- Mac Mini T3Code v0.0.40 at `http://127.0.0.1:3773`, launched by `~/Library/LaunchAgents/com.graham.t3code.plist` through `~/bin/start-t3code.sh`; settings in `~/.t3/userdata/settings.json`.
- Private disposable repository: https://github.com/NorthStar-Technology-Advisory/nor-173-bridge-acceptance, local checkout `/Users/graham/code/northstar/nor-173-bridge-acceptance`, initial pushed commit `62f244e`.
- Disposable Linear project `64cea6c3-3f35-4cc8-beef-b2a7d23c7193`; issue [NOR-186](https://linear.app/northstar-tech-advisory/issue/NOR-186/acceptance-implement-numeric-summary-utility).
- T3Code project `66b83705-22d4-4f8b-a64f-6d059a6bd1b1` maps to that checkout.
- A temporary CLI-issued bearer session is stored in ignored, mode-0600 `data/live-acceptance/t3-session.json` (two-hour expiry). Actual orchestration scopes are `orchestration:read` and `orchestration:operate`; operator documentation was corrected accordingly.

The production adapter successfully read the live project list, created probe thread `bffb0248-af30-4595-9b2e-3957315abacd`, parsed its snapshot and completed authenticated WebSocket replay including the large-gap snapshot fallback. It dispatched command `bd57f4d8-e35f-4b2b-a7f6-21f23f98d2bd` to provider `codex`, model `gpt-5.6-sol`. Turn `01a09fb3-81fb-7f91-9993-36fd8ec74d28` completed with the expected response, “NOR-173 T3Code provider connectivity confirmed.” This probe requested no file edits or tool calls.

The operator created the Linear OAuth app and installed it as app user `d7638c06-739b-4fc4-a0df-0a4fcc42ebfe` (NOR-173 Bridge Acceptance), with `read`, `write`, `app:assignable` and `app:mentionable`. Configuration is in ignored, private `data/live-acceptance/bridge.env`; installation tokens are in `data/live-acceptance/linear-tokens.json`. The acceptance HTTPS tunnel forwards only to the bridge on port 8787; T3Code remains on localhost. This is a temporary acceptance setup, not a deployment deliverable.

### Live-discovered receiver bug

The first real delegation reached the receiver but returned HTTP 400 `invalid_payload`. The operator's Linear delivery-failure screen confirmed the response. Temporary request tracing captured a second initial event: Linear sent `guidance: null` when no guidance existed, whereas the receiver allowed only an array or omission. A signed-webhook regression reproduced 400 instead of 200, then passed after accepting nullable guidance. It also verifies one turn after restart and duplicate delivery. A fresh real delegation for [NOR-196](https://linear.app/northstar-tech-advisory/issue/NOR-196/acceptance-missing-prerequisite-handling) was durably accepted after the fix (session `83cc722f-9e5d-4a23-a56f-0cda482369f1`). Runtime tracing was removed from the running process. Both review axes found no issue with the fix; typecheck, build and the full 32-test suite passed.

### Coding delivery, follow-up and restart

- NOR-186 Agent Session: `678dba98-ba4a-4651-a9d7-7f40413f05f4`. Its first accepted event was an actual prompted webhook from a reply to the session's Linear comment thread, following the rejected initial event.
- T3Code thread: `2049e9a0-0f1b-43a1-ba35-47ba423a181e`.
- Branch: `linear/2049e9a0-0f1b-43a1-ba35-47ba423a181e`.
- Worktree: `data/live-acceptance/worktrees/2049e9a0-0f1b-43a1-ba35-47ba423a181e` under the bridge checkout, separate from the registered repository root.
- Draft PR: https://github.com/NorthStar-Technology-Advisory/nor-173-bridge-acceptance/pull/1.
- Initial implementation commit: `897e9a49b3c6720f25ef9cce966b4e4d47dd45e7`, with six Node tests reported passing.
- Follow-up through a real Linear thread reply added `mean`, using the same session, thread, branch and PR: commit `cd50a7ccfd88978d7a50e30c99a4e8abff976b4b`. Independently reran `npm test` in that worktree: six passed, zero failed. The PR remained open and draft.
- During the initial running turn, the bridge received SIGTERM and restarted against the same database. Snapshots before and after show the same running turn `01a09fc4-2903-7c41-913b-d4964d38c16d` and exactly one user message. The turn then completed and delivered the draft PR. Snapshot evidence is private under `data/live-acceptance/before-restart.json` and `after-restart.json`.

### Cancellation and incomplete-work reporting

The first cancellation probe's three-minute wait finished before the operator reached the control. It correctly reported incomplete work and paused with its follow-up preserved. An ordinary `cancel` reply cleared that queue and stopped the provider, after which a new prompt resumed the same thread for a longer wait.

The operator then selected **Send stop request** in Linear. Activity `3fcd3d9b-77d2-46c5-8607-4ddc5f780408` carries the actual `signal: stop` and was created at `2026-09-14T12:16:20Z`. The bridge posted its stop confirmation at `12:16:24Z`. Verification showed:

- Bridge lifecycle `cancelled`, no queued prompts, no pending responses, and an empty delivery outbox.
- T3Code provider session `stopped`, with no active turn.
- Exactly four user messages in the existing thread: initial implementation, mean follow-up, first wait and repeated wait. The queued test prompt was never submitted.
- `acceptance-cancel-marker.txt` remains untracked with its exact original contents; `acceptance-queued-should-not-exist.txt` does not exist.
- PR #1 remains OPEN and draft at `cd50a7ccfd88978d7a50e30c99a4e8abff976b4b`; branch, worktree and thread are preserved. Private evidence is stored in `data/live-acceptance/after-stop.json`.

NOR-196 also completed its missing-prerequisite probe. T3Code reported the intentionally absent acceptance-policy document, made no implementation changes and opened no PR. The bridge reported incomplete work and paused the session. This verifies that a blocked task is not presented as successful delivery.

A process-level follow-up found the intentional `sleep 1200` still running as an orphan (parent PID 1) after T3Code reported the provider stopped. The acceptance operator verified that exact command and terminated only that disposable wait process. Provider shutdown and bridge cancellation passed, but termination of all descendant processes did not. The bridge delegates interruption/session stop to T3Code; this observed T3Code cancellation limitation remains unresolved and must not be represented as complete process containment.

### Supplementary coverage limits

Native Codex command approval cannot be triggered in this full-access configuration: T3Code configures `approvalPolicy: never` and `sandbox: danger-full-access`. Correlated questions, explicit approvals, ambiguous replies and restart recovery of pending requests remain covered by the controlled integration tests; no live native-approval success is claimed.

Acceptance evidence:

- [x] Delegate a representative mapped issue; record Agent Session, T3Code thread, branch/worktree and draft PR URL.
- [x] Record actual validation commands/results and observed integration versions.
- [x] Send a follow-up; verify the same thread, branch and open PR update.
- [x] Restart during execution; prove no duplicated turn and coherent replay/reporting.
- [x] Cancel via Linear's stop control; verify provider stop, queue clearing and preserved edits.
- [ ] Answer a correlated question and approval; confirm ambiguous replies grant nothing.
- [x] Demonstrate live incomplete-work reporting for missing prerequisites.
- [ ] Exercise live cleanup after PR closure. Controlled integration tests cover cleanup/preservation; the acceptance PR is intentionally retained open for review.

The spec-required core live exercise—draft-PR delivery, same-session follow-up, restart recovery and cancellation with preserved edits—passed on this Mac Mini configuration. This does not claim live native-question/approval or PR-closure cleanup coverage. Those paths have automated coverage, with the live limitations noted above.
