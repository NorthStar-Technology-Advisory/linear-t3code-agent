# NOR-173 acceptance record

## Automated and contract verification

Implementation exercises the signed webhook boundary against controlled Linear HTTP and T3Code HTTP/WebSocket services, SQLite recovery, temporary git repositories and an external PR-host boundary. Coverage includes routing, isolated sessions, duplicate delivery, ordered follow-ups, failure pause/resume, lost acknowledgements, event catch-up, correlated questions/approvals, stop signals, delayed provider stop, context refresh/pagination, explicit missing prerequisites, draft/incomplete reporting, concurrency and conservative cleanup.

Final verification on Node.js 24.18.0:

- `npm run typecheck` — passed for production code and tests.
- `npm test` — 31 passed, 0 failed, 0 skipped.
- `npm run build` — passed.
- `git diff --check` — passed.

Focused tests were run through red/green implementation cycles. The full suite was run once after the review fixes.

Contract sources inspected on 2026-09-14:

- T3Code observed source commit `01e05c15268dedb76da95f442fbf5201cd8e7a44`; `packages/contracts/src/orchestration.ts`, `environmentHttp.ts`, provider ingestion, command receipts, snapshot projection and RPC handlers.
- T3Code's Effect RPC JSON wire protocol (`effect` 4.0.0-rc.112 as observed upstream), used only by the adapter's scoped event catch-up operation; this is not a T3Code compatibility pin.
- Linear's published GraphQL SDL, agent interaction and signal documentation.

## Standards

The independent standards review found no documented-standard violations. Its operational finding (unbounded replay-ticket retention) was fixed. Cancellation and thread-identity checks were consolidated, and definitive versus retryable integration failures now have explicit recovery behavior. The focused re-review confirmed all four findings resolved.

## Spec

The independent spec review found large-gap replay recovery and uncorrectable rejected answers. Both were fixed and covered at the signed-webhook boundary. Re-review identified multi-select answer support; request-specific validation now preserves valid arrays across restart, with a regression test. The final focused re-review found no outstanding code blocker.

Review totals: Standards 0 open findings; Spec 0 open code findings. Live acceptance remains the outstanding requirement.

## Live acceptance — outstanding

Live setup on 2026-09-14:

- Mac Mini T3Code v0.0.40 at `http://127.0.0.1:3773`, launched by `~/Library/LaunchAgents/com.graham.t3code.plist` through `~/bin/start-t3code.sh`; settings in `~/.t3/userdata/settings.json`.
- Private disposable repository: https://github.com/NorthStar-Technology-Advisory/nor-173-bridge-acceptance, local checkout `/Users/graham/code/northstar/nor-173-bridge-acceptance`, initial pushed commit `62f244e`.
- Disposable Linear project `64cea6c3-3f35-4cc8-beef-b2a7d23c7193`; issue [NOR-186](https://linear.app/northstar-tech-advisory/issue/NOR-186/acceptance-implement-numeric-summary-utility).
- T3Code project `66b83705-22d4-4f8b-a64f-6d059a6bd1b1` maps to that checkout.
- A temporary CLI-issued bearer session is stored in ignored, mode-0600 `data/live-acceptance/t3-session.json` (two-hour expiry). Actual orchestration scopes are `orchestration:read` and `orchestration:operate`; operator documentation was corrected accordingly.

The production adapter successfully read the live project list, created probe thread `bffb0248-af30-4595-9b2e-3957315abacd`, parsed its snapshot and completed authenticated WebSocket replay including the large-gap snapshot fallback. It dispatched command `bd57f4d8-e35f-4b2b-a7f6-21f23f98d2bd` to provider `codex`, model `gpt-5.6-sol`. Turn `01a09fb3-81fb-7f91-9993-36fd8ec74d28` completed with the expected response, “NOR-173 T3Code provider connectivity confirmed.” This probe requested no file edits or tool calls.

The available Linear credential cannot manage OAuth applications: the API rejected the application query with `Invalid scope: oauth:create required`. The existing bridge OAuth configuration location has been requested. No real Linear Agent Session has yet been delegated and no coding draft PR has been opened. The connectivity probe does not substitute for the end-to-end checklist below.

Required evidence before declaring v1 ready:

- [ ] Delegate a representative mapped issue; record Agent Session, T3Code thread, branch/worktree and draft PR URL.
- [ ] Record actual validation commands/results and observed integration versions.
- [ ] Send a follow-up; verify the same thread, branch and open PR update.
- [ ] Restart during execution; prove no duplicated turn and coherent replay/reporting.
- [ ] Cancel via Linear's stop control; verify provider stop, queue clearing and preserved edits.
- [ ] Answer a correlated question and approval; confirm ambiguous replies grant nothing.
- [ ] Demonstrate incomplete-work reporting and safe cleanup/preservation after PR closure.

Automated results do not satisfy these live acceptance requirements. V1 readiness remains unverified until this checklist has concrete evidence.
